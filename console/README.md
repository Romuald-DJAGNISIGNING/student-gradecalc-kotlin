# console module

JVM CLI for the same grading rules used by the Android app.

## Run

```bash
./gradlew :console:run --args="--input samples/parity/input_students.csv --output build/exports/grades.xlsx"
```

## Help

```bash
./gradlew :console:run --args="--help"
```
